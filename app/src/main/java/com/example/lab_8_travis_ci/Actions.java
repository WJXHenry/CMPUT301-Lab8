package com.example.lab_8_travis_ci;

public class Actions {
    /**
     * Adds two numbers together
     * @param num1 The first number to add
     * @param num2 The second number to add
     * @return The sum of the two numbers
     */
    public static float Add(float num1, float num2) {
        return num1 + num2;
    }

    /**
     * Subtracts a number from another number
     * @param num1 The number to subtract from
     * @param num2 The number to subtract by
     * @return The different between the first and second number
     */
    public static float Subtract(float num1, float num2) {
        return num1 - num2;
    }

    /**
     * Multiplies two numbers together
     * @param num1 The first number to multiply by
     * @param num2 The second number to multiply by
     * @return The product of the two numbers
     */
    public static float Multiply(float num1, float num2) {
        return 0;
    }
}
